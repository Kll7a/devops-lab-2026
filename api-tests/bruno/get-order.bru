meta {
  name: get order
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/orders/{{orderId}}
  auth: none
}

tests {
  test("заказ найден", function() {
    expect(res.getStatus()).to.equal(200);
  });
  test("тело содержит id", function() {
    expect(res.body.id).to.be.a("string");
  });
}
